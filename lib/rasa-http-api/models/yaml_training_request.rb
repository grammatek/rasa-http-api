=begin
#Rasa - Server Endpoints

#The Rasa server provides endpoints to retrieve trackers of conversations as well as endpoints to modify them. Additionally, endpoints for training and testing models are provided.

The version of the OpenAPI document: 1.0.0

Generated by: https://openapi-generator.tech
OpenAPI Generator version: 6.2.1

=end

require 'date'
require 'time'

module Rasa::HTTP
  class YAMLTrainingRequest
    # Pipeline list
    attr_accessor :pipeline

    # Policies list
    attr_accessor :policies

    # Entity list
    attr_accessor :entities

    # Slots list
    attr_accessor :slots

    # Action list
    attr_accessor :actions

    # Forms list
    attr_accessor :forms

    # E2E Action list
    attr_accessor :e2e_actions

    # Bot response templates
    attr_accessor :responses

    attr_accessor :session_config

    # Rasa NLU data, array of intents
    attr_accessor :nlu

    # Rule list
    attr_accessor :rules

    # Rasa Core stories in YAML format
    attr_accessor :stories

    # Force a model training even if the data has not changed
    attr_accessor :force

    # If `true` (default) the trained model will be saved in the default model directory, if `false` it will be saved in a temporary directory
    attr_accessor :save_to_default_model_directory

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'pipeline' => :'pipeline',
        :'policies' => :'policies',
        :'entities' => :'entities',
        :'slots' => :'slots',
        :'actions' => :'actions',
        :'forms' => :'forms',
        :'e2e_actions' => :'e2e_actions',
        :'responses' => :'responses',
        :'session_config' => :'session_config',
        :'nlu' => :'nlu',
        :'rules' => :'rules',
        :'stories' => :'stories',
        :'force' => :'force',
        :'save_to_default_model_directory' => :'save_to_default_model_directory'
      }
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'pipeline' => :'Array',
        :'policies' => :'Array',
        :'entities' => :'Array',
        :'slots' => :'Array',
        :'actions' => :'Array',
        :'forms' => :'Array',
        :'e2e_actions' => :'Array',
        :'responses' => :'Hash<String, TemplateDescription>',
        :'session_config' => :'YAMLTrainingRequestSessionConfig',
        :'nlu' => :'Array',
        :'rules' => :'Array',
        :'stories' => :'Array',
        :'force' => :'Boolean',
        :'save_to_default_model_directory' => :'Boolean'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `Rasa::HTTP::YAMLTrainingRequest` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!self.class.attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `Rasa::HTTP::YAMLTrainingRequest`. Please check the name to make sure it's valid. List of attributes: " + self.class.attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'pipeline')
        self.pipeline = attributes[:'pipeline']
      end

      if attributes.key?(:'policies')
        self.policies = attributes[:'policies']
      end

      if attributes.key?(:'entities')
        self.entities = attributes[:'entities']
      end

      if attributes.key?(:'slots')
        self.slots = attributes[:'slots']
      end

      if attributes.key?(:'actions')
        self.actions = attributes[:'actions']
      end

      if attributes.key?(:'forms')
        self.forms = attributes[:'forms']
      end

      if attributes.key?(:'e2e_actions')
        self.e2e_actions = attributes[:'e2e_actions']
      end

      if attributes.key?(:'responses')
        if (value = attributes[:'responses']).is_a?(Hash)
          self.responses = value
        end
      end

      if attributes.key?(:'session_config')
        self.session_config = attributes[:'session_config']
      end

      if attributes.key?(:'nlu')
        self.nlu = attributes[:'nlu']
      end

      if attributes.key?(:'rules')
        self.rules = attributes[:'rules']
      end

      if attributes.key?(:'stories')
        self.stories = attributes[:'stories']
      end

      if attributes.key?(:'force')
        self.force = attributes[:'force']
      end

      if attributes.key?(:'save_to_default_model_directory')
        self.save_to_default_model_directory = attributes[:'save_to_default_model_directory']
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      invalid_properties = Array.new
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      true
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          pipeline == o.pipeline &&
          policies == o.policies &&
          entities == o.entities &&
          slots == o.slots &&
          actions == o.actions &&
          forms == o.forms &&
          e2e_actions == o.e2e_actions &&
          responses == o.responses &&
          session_config == o.session_config &&
          nlu == o.nlu &&
          rules == o.rules &&
          stories == o.stories &&
          force == o.force &&
          save_to_default_model_directory == o.save_to_default_model_directory
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [pipeline, policies, entities, slots, actions, forms, e2e_actions, responses, session_config, nlu, rules, stories, force, save_to_default_model_directory].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      attributes = attributes.transform_keys(&:to_sym)
      self.class.openapi_types.each_pair do |key, type|
        if attributes[self.class.attribute_map[key]].nil? && self.class.openapi_nullable.include?(key)
          self.send("#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            self.send("#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          self.send("#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = Rasa::HTTP.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end

  end

end
